# Email address used for the initial administrator account.
ADMIN_EMAIL=owner@example.com

# SHA-256 hash of the one-time setup token. Configure this as a hosted secret.
ADMIN_SETUP_TOKEN_HASH=replace-with-sha256-hash
